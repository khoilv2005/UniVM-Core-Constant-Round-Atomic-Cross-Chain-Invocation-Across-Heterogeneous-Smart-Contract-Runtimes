// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

// AUTO-GENERATED by UBTL Ψ_EVM; do not edit.
// Source IR hash: 0x4af67efbeef93d68a237778e054e8c10f69f41366099aa4b223027bff80ecafe
// Source VM: Wasm(rustc 1.85.1)
contract TrainBookingTranslated {
    error UbtlUnsupported(string reason);
    bytes32 public constant UBTL_IR_HASH = bytes32(0x4af67efbeef93d68a237778e054e8c10f69f41366099aa4b223027bff80ecafe);

    struct IrHashSlot {
        bytes field0;
    }

    address private bridge;
    uint256 private price;
    uint256 private remain;
    IrHashSlot private irHash;
    uint256 private lockedAmount;
    uint256 private lockBlock;
    uint256 private timeoutBlocks;
    bool private active;
    uint256 private lockedTotal;
    uint256 private accounts;
    uint256 private bookings;
    uint256 private lockSize;

    constructor() {}


    function __ubtl_available_units() internal view returns (uint256) {
        if (price == 0) {
            return 0;
        }
        uint256 lockedUnits = lockedTotal / price;
        return remain > lockedUnits ? remain - lockedUnits : 0;
    }

    function ir_hash() external view returns (bytes memory) {
        return abi.encodePacked(UBTL_IR_HASH);
    }

    function get_price() external view returns (uint256) {
        return price;
    }

    function get_remain() external view returns (uint256) {
        return remain;
    }

    function get_available_remain() external view returns (uint256) {
        return __ubtl_available_units();
    }

    function get_account_balance(address) external pure returns (uint256) {
        revert UbtlUnsupported("function get_account_balance is outside the prototype subset");
    }

    function get_booking(address) external pure returns (uint256) {
        revert UbtlUnsupported("function get_booking is outside the prototype subset");
    }

    function is_state_locked(uint256) external pure returns (bool) {
        revert UbtlUnsupported("function is_state_locked is outside the prototype subset");
    }

    function vassp_encode_state(uint256) external pure returns (bytes memory) {
        revert UbtlUnsupported("function vassp_encode_state is outside the prototype subset");
    }

    function book_local(address, uint256) external pure returns (uint256) {
        revert UbtlUnsupported("function book_local is outside the prototype subset");
    }

    function lock_state(uint256, uint256, uint256) external pure returns (bytes memory) {
        revert UbtlUnsupported("function lock_state is outside the prototype subset");
    }

    function update_state(uint256, uint256, address, uint256, uint256) external pure returns (bytes memory) {
        revert UbtlUnsupported("function update_state is outside the prototype subset");
    }

    function unlock_state(uint256) external pure returns (bytes memory) {
        revert UbtlUnsupported("function unlock_state is outside the prototype subset");
    }

    function unlock_on_timeout(uint256) external pure returns (bytes memory) {
        revert UbtlUnsupported("function unlock_on_timeout is outside the prototype subset");
    }

    function set_bridge(address arg0) external returns (bytes memory) {
        bridge = arg0;
    }

    function __vassp_apply(bytes32 slot, bytes calldata value) external {
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "bridge"))) {
            bridge = abi.decode(value, (address));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "price"))) {
            price = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "remain"))) {
            remain = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "locked_amount"))) {
            lockedAmount = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "lock_block"))) {
            lockBlock = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "timeout_blocks"))) {
            timeoutBlocks = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "active"))) {
            active = abi.decode(value, (bool));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "locked_total"))) {
            lockedTotal = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "accounts"))) {
            accounts = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "bookings"))) {
            bookings = abi.decode(value, (uint256));
            return;
        }
        if (slot == keccak256(abi.encodePacked("VASSP", "train_booking", "lock_size"))) {
            lockSize = abi.decode(value, (uint256));
            return;
        }
        revert UbtlUnsupported("__vassp_apply currently supports scalar and meta slots only");
    }
}
